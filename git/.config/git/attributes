# Auto detect text files and perform LF normalisation.
*   text=auto

# Shell Scripts
*.sh    text eol=lf
*.zsh   text eol=lf
*.bash  text eol=lf
*.fish  text eol=lf

# Windows
*.bat   text eol=crlf
*.cmd   text eol=crlf
*.ps1   text eol=crlf

# Lock Files
*.lock              text -diff
package-lock.json   text -diff

# vim:ft=gitattributes
